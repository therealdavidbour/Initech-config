apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: orders-api-{{ .Values.branch | trimPrefix "env/" }}
  namespace: argocd
spec:
  project: default
  source:
    repoURL: {{ .Values.repoURL }}
    targetRevision: {{ .Values.branch }}
    path: services/orders-api/templates
  destination:
    server: https://kubernetes.default.svc
    namespace: {{ .Values.branch | trimPrefix "env/" }}
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
