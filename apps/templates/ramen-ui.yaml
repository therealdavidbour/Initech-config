apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ramen-ui-{{ .Values.branch | trimPrefix "env/" }}
  namespace: argocd
spec:
  project: default
  source:
    repoURL: {{ .Values.repoURL }}
    targetRevision: {{ .Values.branch }}
    path: services/ramen-ui/templates
  destination:
    server: https://kubernetes.default.svc
    namespace: {{ .Values.branch | trimPrefix "env/" }}
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
